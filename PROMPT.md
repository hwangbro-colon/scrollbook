# 북북(BookBook) 프로토타입 개발 프롬프트 — Claude Code용

아래 내용을 그대로 Claude Code에 붙여넣어 사용하세요.

---

## 1. 프로젝트 개요

북북(BookBook)은 청소년 문해력 저하를 스크롤 기반 독서로 해결하는 모바일 앱입니다.
이번 프로토타입은 **실제 출시 범위(스크롤 기능만)** 를 기준으로 한 인터랙티브 HTML 프로토타입입니다.
음성 낭독, 그룹 읽기, 독서 보조 기능은 이번 스코프에서 제외합니다.

- iPhone 목업 프레임 안에서 동작하는 인터랙티브 HTML 프로토타입으로 제작
- 실제 백엔드/인증 없이 목 데이터로 동작 (탭 전환, 스크롤, 화면 이동 등 인터랙션은 실제 동작해야 함)

## 2. 화면 구성 (하단 탭 4개 + 온보딩 플로우)

### 하단 탭바 (4탭, 전 화면 공통 고정)
`북북(홈)` / `책장` / `(확장 — 비활성, "곧 만나요" 플레이스홀더)` / `프로필`

### 온보딩 플로우
1. **스플래시**: 흰 배경, 중앙 "북북" 로고
2. **회원가입 및 로그인**: 이메일/닉네임 입력 필드 + 로그인 버튼 (실제 인증 없이 다음 화면으로 이동)
3. **사용법 안내 팝업**: 북북 홈 진입 시 최초 1회 표시
   - 카피: "책을 스크롤 해서 읽으세요!"
   - 카피: "첫 5문장이 마음에 들면 계속 읽고, 아니면 위 책장으로 가서 다른 책을 고르세요"
   - "닫기" 버튼으로 dismiss

### 탭 1 — 북북 (홈, 스크롤 리딩)
- 청크 단위로 텍스트가 스크롤되는 완독스크롤 리딩 화면 (목 데이터 책 1권 이상)
- 상단: "북북" 로고 + 진행률 "N/N" (읽은 문단 수 / 전체 문단 수)
- 완독 시: 중앙에 "the end" 및 완독 축하 상태 표시

### 탭 2 — 책장
- 검색창 (플레이스홀더, 실제 검색 로직 불필요)
- 필터 탭: 전체 / 소설 / 고전 / 로맨스 (클릭 시 목 데이터 필터링)
- 책 캐러셀: 가로 스크롤, 중앙 카드 강조
- 책 카드 정보: 책 제목 + "스크롤 수 / 완독 수 / 클릭 수"
- 진행 상태 태그: "거의 다 읽은" / "완독"
- 통계 요약 박스 3개 (예: 완독률, 참여율 등 퍼센트)

### 탭 3 — (확장)
- 비활성 placeholder 화면: "곧 만나요" 또는 유사 카피만 표시 (추후 기능 자리)

### 탭 4 — 프로필
- 프로필 사진 등록 영역 (플레이스홀더 이미지 + 편집 아이콘)
- "N권 스크롤" 완독 마일리지 버튼
- 닉네임 설정 필드
- 자기소개/목표 텍스트 박스
- "총 기록" 섹션: 완독한 책 리스트 (제목 + 완독 날짜)
- 설정 진입점 (우측 상단 아이콘) → 하위 화면:
  - **설정 화면**: 알림설정(토글), 버전, 문의, 인스타그램 링크

## 3. 사용자 프로필 화면 요구사항 (필수 반영)
- 프로필 사진 등록 (업로드/변경 UI)
- 닉네임 설정
- 완독 권수 통계: **한달 / 1년 / 전체** 구분 표시 (탭 또는 세그먼트 컨트롤로 전환)
- 개인정보(닉네임 외 이메일 등)는 기본 마스킹(`**`) 처리, 탭하면 전체 값이 보이는 토글 방식

## 4. 브랜드 컬러 & 디자인 톤
- **색상: 흰색 / 검정 / 주황 3색만 사용** (다른 색상 팔레트 사용 금지)
- 기본: 흰 배경 + 검은 텍스트, 주황은 포인트 컬러(강조 버튼, 활성 탭, 진행률 등)로 제한적 사용
- **버튼: border-radius 0 (완전 각짐, 예외 없음)**
- 버튼 외 요소(카드, 아바타, 통계 박스 등): 8~10px 라운드 유지
- 톤앤매너: BBC 웹사이트 + 듀오링고 앱을 섞은 느낌, 과하게 세련되지 않게
- 2D 요소 위주 + 3D 요소는 소량만 가미
- 스크롤바는 숨김 처리

## 5. 폰트
- **부크크 고딕 Bold/Light** 사용 (로고, 버튼, 카피, 본문 전체)
- 폰트 파일이 없을 경우 유사 두께의 시스템 산세리프로 임시 대체하고, 어디에 실제 폰트 파일을 넣어야 하는지 주석으로 명시

## 6. 아키텍처 원칙 (4가지, 반드시 준수)
1. 로고는 `/public/image.files/logo.png` 파일 하나만 교체하면 전체 앱에 반영되는 구조
2. 색상/텍스트/카피는 `/src/config/theme.js` 단일 파일에서 중앙 관리 (컴포넌트에 하드코딩 금지)
3. 개발 순서: 내비게이션 뼈대(탭 전환) → 각 탭 목 데이터 화면 → 인터랙션(스크롤/필터/토글) → 세부 디테일. 뼈대 우선, 단계적으로 살을 붙일 것
4. UI 컴포넌트를 먼저 설계하고, 화면은 그 컴포넌트들을 조합하는 방식으로 구성 (재사용 가능한 Button, Card, Tab, StatBox 등 컴포넌트 우선 정의)

## 7. 코드/용량 최적화 원칙 (8가지, 항상 적용)
1. 대용량 텍스트(책 본문 등)는 실제로는 CDN에서 온디맨드로 불러온다고 가정하고 목 데이터 구조를 그렇게 짤 것. 메타데이터(index.json)만 가볍게 로드, 장편은 페이지네이션/무한스크롤 패턴 적용
2. 이미지는 WebP/AVIF + 반응형 처리 + lazy loading 가정. 아바타/뱃지는 표시 크기 썸네일만 사용, 뱃지류는 SVG 재사용 우선
3. 폰트는 실제 사용하는 웨이트(Bold/Light)만 로드
4. (낭독 관련 없음 — 이번 스코프 제외)
5. 코드는 화면(탭)별로 분리 가능한 구조로 작성 (lazy loading 가능하도록). 무거운 라이브러리는 필요한 것만 부분 import. 애니메이션은 CSS 우선 사용
6. 책 목록/캐러셀은 클라이언트에서 전체 목록을 한 번에 렌더링하지 말고, 페이지네이션/정렬된 형태로 필요한 만큼만 그리는 구조로 작성
7. 상태 관리는 Redux 대신 useState/useReducer 또는 Context 사용
8. 최종 코드에는 불필요한 console.log, 미사용 CSS 남기지 않기

## 8. 참고: 스토리보드 원본 요약
`spec/STORYBOARD.md` 파일에 Canva 스토리보드 7화면 전체 내용이 정리되어 있습니다. 화면별 카피/레이아웃은 이 파일을 우선 참고하세요.

---

위 내용을 기준으로 iPhone 목업 프레임 안에 들어가는 인터랙티브 HTML 프로토타입을 만들어 주세요. 탭 전환과 스크롤 인터랙션이 실제로 동작해야 합니다.
